'use client';
import Image from 'next/image';import { useState,useEffect } from 'react';
export function Gallery({name,images}:{name:string;images:string[]}){const[i,setI]=useState<number|null>(null);useEffect(()=>{const h=(e:KeyboardEvent)=>{if(i===null)return;if(e.key==='Escape')setI(null);if(e.key==='ArrowRight')setI((v)=>v===null?0:(v+1)%images.length);if(e.key==='ArrowLeft')setI((v)=>v===null?0:(v-1+images.length)%images.length)};window.addEventListener('keydown',h);return()=>window.removeEventListener('keydown',h)},[i,images.length]);return <><div className='columns-1 gap-6 md:columns-2 lg:columns-3'>{images.map((src,idx)=><button key={idx} onClick={()=>setI(idx)} className='group mb-6 block w-full text-left'><div className='relative overflow-hidden'><Image src={src} alt={`${name} artwork ${idx+1}`} width={900} height={1200} className='h-auto w-full transition duration-700 group-hover:scale-[1.03]'/></div></button>)}</div>{i!==null&&<div className='fixed inset-0 z-[70] flex items-center justify-center bg-black/85 p-6' onClick={()=>setI(null)}><Image src={images[i]} alt={`${name} enlarged`} width={1200} height={1600} className='max-h-[90vh] w-auto'/></div>}</>}
