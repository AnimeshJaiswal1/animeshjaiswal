'use client';
import Image from 'next/image';import Link from 'next/link';import { categories } from '@/data/site';import { motion } from 'framer-motion';
export function CategoryGrid(){return <section className='container-x grid gap-6 md:grid-cols-2'>{categories.map((c,i)=><motion.div key={c.slug} initial={{opacity:0,y:30}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*0.08,duration:0.8}}><Link href={`/artwork/${c.slug}`} className='group relative block overflow-hidden'><div className='relative aspect-[4/3]'><Image src={c.hero} alt={c.name} fill className='object-cover transition duration-700 group-hover:scale-105'/></div><div className='absolute inset-0 bg-black/20'/><p className='serif absolute bottom-6 left-6 text-3xl text-white'>{c.name}</p></Link></motion.div>)}</section>}
